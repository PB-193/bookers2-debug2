<tr>
  <td>
    <p>コメント件数：<%= book.book_comments.count %></p>
    <% book.book_comments.each do |book_comment| %>
      <p><%= image_tag book_comment.user.get_profile_image, size:'100x100'  %></p>
      <p>ユーザ名　<%= book_comment.user.name %></p>
      <p>コメントした日 <%= book_comment.created_at.strftime('%Y/%m/%d') %></p>
      <p>コメント内容　<%= book_comment.comment %></p>
      <p>
      <% if book_comment.user == current_user %>
        <%= link_to "Destroy", book_book_comment_path(book_comment.book, book_comment), method: :delete, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger" %>
      <% end %>
      </p>
    <!--(book_comment.book, book_comment)の意味=>book_comment.bookは上の「book_comment」の引数のbookとコメントのURLをメソッドに記載-->
    <% end %>
  </td>
</tr>